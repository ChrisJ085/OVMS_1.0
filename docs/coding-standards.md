# OVMS Coding Standards

- Use React with functional components and hooks.
- Strict TypeScript types are required everywhere.
- Do not create silent fallback behaviour that hides errors.
- Keep business logic outside of large UI components.
- Use Tailwind CSS for all styling, avoid inline styles.
- Create reusable services for domain logic and data access.
- Modular folder structure must be followed for all features.
