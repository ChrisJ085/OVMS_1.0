--- src/features/planning/pages/ProductionPlanPage.tsx
+++ src/features/planning/pages/ProductionPlanPage.tsx
@@ -82,9 +82,9 @@
     // Determine current week's Monday
     const today = new Date();
     const day = today.getDay();
     const diff = today.getDate() - day + (day === 0 ? -6 : 1);
-    const d = new Date(today.setDate(diff));
-    d.setHours(0,0,0,0);
+    // Create a UTC date for the start of the week
+    const d = new Date(Date.UTC(today.getFullYear(), today.getMonth(), diff, 0, 0, 0, 0));
     return d;
   });
@@ -303,6 +303,6 @@
         const startTimestamp = Timestamp.fromDate(new Date(currentWeekStart.getTime() - 24 * 60 * 60 * 1000));
         const endWeek = new Date(currentWeekStart.getTime() + 8 * 24 * 60 * 60 * 1000);
         const endTimestamp = Timestamp.fromDate(endWeek);
@@ -417,6 +417,6 @@
     const d = new Date(currentWeekStart);
-    d.setDate(currentWeekStart.getDate() + i);
+    d.setUTCDate(currentWeekStart.getUTCDate() + i);
     return d;
   });
@@ -661,5 +661,16 @@
+                  const endWeekUTC = new Date(currentWeekStart.getTime() + 6 * 24 * 60 * 60 * 1000);
+                  const formatUTCDate = (d: Date) => {
+                     const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
+                     return `${d.getUTCDate()} ${months[d.getUTCMonth()]}`;
+                  };
+                  const formatUTCFull = (d: Date) => {
+                     const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
+                     return `${d.getUTCDate()} ${months[d.getUTCMonth()]} ${d.getUTCFullYear()}`;
+                  };
                 <span className="font-semibold text-slate-200">
-                  Planning Window: {currentWeekStart.toLocaleDateString(undefined, { day: 'numeric', month: 'short' })} - {new Date(currentWeekStart.getTime() + 6 * 24 * 60 * 60 * 1000).toLocaleDateString(undefined, { day: 'numeric', month: 'short', year: 'numeric' })}
+                  Planning Window: {formatUTCDate(currentWeekStart)} - {formatUTCFull(endWeekUTC)}
                 </span>
@@ -669,4 +680,4 @@
                 <Button variant="outline" size="sm" onClick={() => {
                   const d = new Date(currentWeekStart);
-                  d.setDate(d.getDate() - 7);
+                  d.setUTCDate(d.getUTCDate() - 7);
                   setCurrentWeekStart(d);
@@ -693,4 +704,4 @@
                 <Button variant="outline" size="sm" onClick={() => {
                   const d = new Date(currentWeekStart);
-                  d.setDate(d.getDate() + 7);
+                  d.setUTCDate(d.getUTCDate() + 7);
                   setCurrentWeekStart(d);
@@ -717,6 +728,10 @@
                       {gridDates.map((date, idx) => (
                         <th key={idx} className="p-4 text-xs font-semibold text-slate-400 uppercase tracking-wider text-center border-l border-slate-850">
-                          <div>{date.toLocaleDateString(undefined, { weekday: 'short' }).toUpperCase()}</div>
-                          <div className="text-slate-500 font-normal">{String(date.getDate()).padStart(2, '0')}/{String(date.getMonth() + 1).padStart(2, '0')}</div>
+                          <div>{['SUN','MON','TUE','WED','THU','FRI','SAT'][date.getUTCDay()]}</div>
+                          <div className="text-slate-500 font-normal">{String(date.getUTCDate()).padStart(2, '0')}/{String(date.getUTCMonth() + 1).padStart(2, '0')}</div>
                         </th>
@@ -774,7 +789,7 @@
                                     const dDate = d.date.toDate();
-                                    return dDate.getDate() === date.getDate() &&
-                                           dDate.getMonth() === date.getMonth() &&
-                                           dDate.getFullYear() === date.getFullYear();
+                                    return dDate.getUTCDate() === date.getUTCDate() &&
+                                           dDate.getUTCMonth() === date.getUTCMonth() &&
+                                           dDate.getUTCFullYear() === date.getUTCFullYear();
                                   });
@@ -816,7 +831,7 @@
                                     const dDate = d.date.toDate();
-                                    return dDate.getDate() === date.getDate() &&
-                                           dDate.getMonth() === date.getMonth() &&
-                                           dDate.getFullYear() === date.getFullYear();
+                                    return dDate.getUTCDate() === date.getUTCDate() &&
+                                           dDate.getUTCMonth() === date.getUTCMonth() &&
+                                           dDate.getUTCFullYear() === date.getUTCFullYear();
                                   });
